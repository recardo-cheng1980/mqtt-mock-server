{
  "name": "mqtt-mock-server",
  "version": "1.3.0",
  "scripts": {
    "start": "pm2 start mqtt.js --name 'mqtt-mtls-broker' --output ./logs/out.log --error ./logs/error.log --time --merge-logs",
    "stop": "pm2 stop mqtt-mtls-broker",
    "status": "pm2 status",
    "log": "pm2 logs mqtt-mtls-broker"
  },
  "dependencies": {
    "aedes": "^1.0.1",
    "express": "^5.2.1",
    "pm2": "^6.0.14"
  }
}
